import { X } from 'lucide-react'
import { CategoryManager } from './CategoryManager'

interface CategoryManageModalProps {
  isOpen: boolean
  onClose: () => void
  userId: string
  onUpdate?: () => void
}

export function CategoryManageModal({ isOpen, onClose, userId, onUpdate }: CategoryManageModalProps) {
  if (!isOpen) return null

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50">
      <div className="bg-white rounded-lg shadow-xl max-w-3xl w-full max-h-[90vh] overflow-hidden flex flex-col">
        {/* Header */}
        <div className="flex items-center justify-between p-6 border-b">
          <h2 className="text-xl font-semibold text-gray-900">Gestisci Categorie</h2>
          <button
            onClick={onClose}
            className="text-gray-400 hover:text-gray-600"
          >
            <X className="w-6 h-6" />
          </button>
        </div>

        {/* Content */}
        <div className="flex-1 overflow-y-auto p-6">
          <CategoryManager userId={userId} onUpdate={onUpdate} />
        </div>

        {/* Footer */}
        <div className="flex items-center justify-end gap-3 p-6 border-t bg-gray-50">
          <button
            onClick={onClose}
            className="px-4 py-2 text-sm font-medium text-white bg-blue-600 rounded-lg hover:bg-blue-700"
          >
            Chiudi
          </button>
        </div>
      </div>
    </div>
  )
}
