# Supabase Configuration (Frontend)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Google Calendar API (Frontend - safe to expose)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Microsoft Calendar API (Frontend - safe to expose)
VITE_MICROSOFT_CLIENT_ID=your_microsoft_client_id_here

# App Configuration (Frontend)
VITE_APP_URL=http://localhost:3000
VITE_TRIAL_DAYS=14

# Stripe (Frontend)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# ==================================================================
# SERVER-SIDE ONLY VARIABLES (for Vercel/Netlify serverless functions)
# These should NEVER be prefixed with VITE_ (frontend won't access them)
# Set these in your deployment platform's environment variables
# ==================================================================

# Google Calendar API (Server-side)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Microsoft Calendar API (Server-side)
MICROSOFT_CLIENT_ID=your_microsoft_client_id_here
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here

# Supabase (Server-side)
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ==================================================================
# IMPORTANT SECURITY NOTES:
# - Never commit .env file to git (already in .gitignore)
# - VITE_ prefixed vars are exposed to frontend (only use for public data)
# - Non-VITE_ vars are server-side only (use for secrets)
# - Get SUPABASE_SERVICE_ROLE_KEY from Supabase Dashboard > Settings > API
# ==================================================================
